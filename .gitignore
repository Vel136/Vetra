# Tests, benchmarks and profilers are kept local. They are Studio-bound (they
# require a running Play session and a synced place), their timings are
# machine-specific, and tests/Profiling vendors third-party libraries whose
# redistribution is a licensing decision of its own.
tests/

# Moonwave build output — the deployed site lives on the gh-pages branch.
build/
.docusaurus/
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
